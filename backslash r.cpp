    #include <stdio.h>
    int main()
    {
        printf("sanfoundry\rclass\n");
        return 0;
    }
// \r moves the cursor to the starting and starts overwritting the next word onto it. Like in this, Sanfoundary will be overwritten by class so it turns out to be classundary.
